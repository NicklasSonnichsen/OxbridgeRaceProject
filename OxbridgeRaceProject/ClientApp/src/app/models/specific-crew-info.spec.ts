import { SpecificCrewInfo } from './specific-crew-info';

describe('SpecificCrewInfo', () => {
  it('should create an instance', () => {
    expect(new SpecificCrewInfo("",0,0)).toBeTruthy();
  });
});
